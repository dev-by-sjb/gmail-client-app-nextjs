import Body from "@/components/SelectedEmailColumn";
import { Sidebar } from "lucide-react";

const MailPage = ({ params }: { params: { slug: string } }) => {
  // console.log(params

  return <div className="bg-pink-500">user page</div>;
};
export default MailPage;
